#ifndef _ArrayStack_H
typedef struct ArrayStack{
	int top; //index of top element, to be initialised with -1
	int capacity; //no of elements in array
	int* Array; //to be dynamically initialised using malloc, with capacity size
}ArrayStack;
#endif
