#ifndef HEAPDEF_H_
	#define HEAPDEF_H_
	#include"heapDef.h"
#endif
heap* createHeap(int capacity, int type);
void insert(heap* h, int data);
int deleteType(heap* h);
int getType(heap* h);
void buildHeap(heap* h, int A[], int n);
void bubbleDown(heap* h, int i);
int max(int A[], int a,int b, int max_ind);
void display(heap* h);
void delHeap(heap* h);

